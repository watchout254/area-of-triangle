const base = window.prompt("Enter the base of the triangle:");
const height = window.prompt("Enter the height of triangle:");

const area = 1/2 * base * height;

document.getElementById("Area").innerHTML = 1/2 * base *height;